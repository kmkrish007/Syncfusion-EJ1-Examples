@Html.EJS().TreeView("listdata").Fields(field =>
    field.Id("id").ParentID("parentId").Selected("selected").
    Expanded("expanded").Text("name").HasChildren("hasChild")
    .DataSource(dataManager => { dataManager.Url("/Home/initialData").Adaptor("UrlAdaptor"); })).AllowDragAndDrop(true).AllowEditing(true).NodeDragStart("DragStart").NodeDragStop("DragStart").NodeDropped("NodeDropped").Render()

<script>
    function BeforeEdit(args) {
       
    }
    function DragStart(args) {
        console.log(args)
    }
    function NodeDropped(args) {
       alert("Node Dropped successfully")
    }
</script>